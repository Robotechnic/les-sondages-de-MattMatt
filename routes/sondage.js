const express = require("express")
const router = express.Router()

module.exports = (db) =>{
	router.get("/sondage/:token",(req,res)=>{
	
	})
	
	router.get("/sondage/:token/response",(req,res)=>{
	
	})
	return router
}