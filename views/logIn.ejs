<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8"/>
	<title>Les sondages de MattMatt - Log In</title>
	<%- include ("parts/include.ejs") %>
</head>
<body>
	<%- include ("parts/header.ejs") %>
	<section class="form-contener">
			<form action="" method="POST" autocomplete="on" class="form-contener__form">
			<h2>Connexion</h2>
			<% if (locals.error) 
				if (error.length > 0) {%>
				<div class="instruction-contener">
					<label class="instruction-contener__instruction"><%= error%></label>
				</div>
			<% } %>
				<label For="pseudo">Pseudo:</label>
				<input 	type="text"
						name="pseudo" 
						id="pseudo" 
						required 
						minlength="4" 
						autofocus
						placeholder="Pseudo"
						value="<%if(locals.pseudo){%><%=pseudo%><%}%>"
				>
				<label For="pseudo">Mot de passe:</label>
				<input 	type="password" 
						name="password" 
						id="password"
						required 
						minlength="8"
						placeholder="Mot de passe"
				>
				<input type="submit" name="submit" value="Valider">
			</form>
	</section>
	<%- include ("parts/footer.ejs") %>
</body>
</html>